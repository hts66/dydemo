# FastAPI + LangChain Agent Service
# =====================================
# 注意: LangChain 生态包之间有严格的版本依赖关系
# 使用 >= 让 pip 自动解决兼容性, 比固定版本更稳定

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.0.0

# LangChain 生态 (按正确顺序安装)
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain>=0.3.0
langgraph>=0.2.0
langchain-community>=0.3.0

# 向量数据库
qdrant-client>=1.12.0

# HTTP 客户端
httpx>=0.27.0

# 工具
python-dotenv>=1.0.0
pydantic>=2.0.0
